#!/bin/bash
# Set execute permission for every file in the current directory
for file in *; do
    if [[ -f "$file" ]]; then
        chmod u+x "$file"
        echo "Added execute permission to: $file"
    fi
done
